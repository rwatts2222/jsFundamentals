/*
    FUNCTIONS

        - functions are a chunk of code that perform a set of actions when called (or invoked)
        - functions are reusable - we can call them as many times as we want and they can take in different sets of data
*/

// function declaration
function hi() {
    console.log('hello');
}

hi();  // function must be called by its name if we want the function to run